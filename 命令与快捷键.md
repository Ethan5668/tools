# Windows 命令与快捷键

## 1. 系统

### 窗口操作

| 快捷键 | 功能 |
|--------|------|
| `Win + ↑/↓/←/→` | 窗口贴靠/最大化/最小化 |
| `Win + Shift + ↑/↓/←/→` | 跨显示器移动窗口 |
| `Alt + F4` | 关闭当前应用 |
| `Win + M` / `Win + Shift + M` | 最小化 / 还原所有窗口 |
| `Ctrl + Shift + Esc` | 直接打开任务管理器 |
| `Win + E` | 打开文件资源管理器 |
| `Win + Shift + S` | 截图 |
| `Shift + 滚轮` | 水平滚动 |

### 提权（也可打开开发者模式）

```powershell
Start-Process pwsh -Verb RunAs -ArgumentList "-Command", "New-Item -ItemType SymbolicLink -Path 'E:\unity-3d\Assets\Plugins\android\lib\arm64-v8a\libmap_view_controller.so' -Target 'E:\ni-map-render\mapcore\output\android\Release\lib\libmap_view_controller.so'" -Wait
```

---

## 2. Vim

### 编辑

- 删除所有不包含 `hello` 的行：`v/hello/d`

### 命令行粘贴

在 Vim 命令行中按 `Ctrl + R`，然后按 `+`（系统剪贴板寄存器），即可粘贴内容。

---

## 3. Git

- 合并多个提交（交互式变基）：

```bash
git rebase -i [HEAD~3 | 哈希值]
# pick -> s（squash）  或  pick -> f（fixup）
```

---

## 4. ADB

```bash
# 启动 Activity
adb shell am start -n com.navinfo.maps.navi/com.navinfo.maps.main.MainActivity

# crash 日志符号解析
adb logcat -b crash | ndk-stack -sym nimapsdk\lib_nimap_common\build\intermediates\cmake\debug\obj\arm64-v8a\

# 截图保存到桌面
adb exec-out screencap -p > ~\Desktop\2.png
```

---

## 5. Android

```bash
./gradlew zipSdkPackage
./gradlew installDebug --parallel --max-workers=8
```

```bash
# addr2line 符号解析
.\aarch64-linux-android-addr2line.exe -f -e E:\ni-map-render\mapcore\output\android\Release\lib\libmap_view_controller.so \
  0000000001474f0c 000000000142d6d8 000000000142c1e0 000000000142125c
```

### 调试面板中 `wchar_t` 转 `char`

```cpp
const char* debug_w(const wchar_t* ws) {
    static thread_local std::string utf8;
    utf8.clear();

    while (*ws) {
        uint32_t c = *ws++;
        if (c < 0x80) utf8 += char(c);
        else if (c < 0x800) {
            utf8 += char(0xC0 | (c >> 6));
            utf8 += char(0x80 | (c & 0x3F));
        } else if (c < 0x10000) {
            utf8 += char(0xE0 | (c >> 12));
            utf8 += char(0x80 | ((c >> 6) & 0x3F));
            utf8 += char(0x80 | (c & 0x3F));
        } else {
            utf8 += char(0xF0 | (c >> 18));
            utf8 += char(0x80 | ((c >> 12) & 0x3F));
            utf8 += char(0x80 | ((c >> 6) & 0x3F));
            utf8 += char(0x80 | (c & 0x3F));
        }
    }
    return utf8.c_str();
}
```

---

## 6. VSCode

### IntelliSense — `compile_commands.json`

`.vscode/c_cpp_properties.json` 配置示例：

```json
{
    "configurations": [
        {
            "name": "android",
            "defines": [],
            "compilerPath": "${env:ANDROID_NDK_HOME}\\toolchains\\llvm\\prebuilt\\windows-x86_64\\bin\\clang++.exe",
            "cStandard": "c17",
            "cppStandard": "c++17",
            "intelliSenseMode": "${default}",
            "compileCommands": [
                "${workspaceFolder}\\MapViewAndroid\\lib_nimap_common\\.cxx\\tools\\debug\\arm64-v8a\\compile_commands.json"
            ]
        }
    ],
    "version": 4
}
```

### 编辑器红线问题（文件编码不统一）

- 设置 `Files: Encoding` 为 `GB2312` 或 `UTF-8`

### CMake 宏定义无效导致代码无法跳转/补全

**原因**：`vscode-cmake-tools` 会自动追加 `-DCMAKE_EXPORT_COMPILE_COMMANDS:BOOL=ON`，编译器将其识别为宏定义导致编译失败。

**解决方法**：在 `CMakePresets.json` 中显式添加：

```json
"cacheVariables": {
    "CMAKE_EXPORT_COMPILE_COMMANDS": "ON"
}
```

---

## 7. Unity

### 命令行导出 Android 工程

```powershell
& "C:\Program Files\Unity\Hub\Editor\2022.3.62f1c1\Editor\Unity.exe" `
    -batchmode -quit `
    -projectPath "E:/unity-3d" `
    -executeMethod BuildAndroidProject.Export `
    -logFile E:/unity_build.log
Get-Content E:/unity_build.log -Wait
```

---

## 8. PowerShell

### `$PROFILE` 配置

`$PROFILE` 变量指向当前用户的 PowerShell 配置文件路径，可在其中添加以下历史记录增强配置：

```powershell
Import-Module PSReadLine
Set-PSReadLineOption -PredictionSource History
Set-PSReadLineOption -PredictionViewStyle ListView
Set-PSReadLineKeyHandler -Key UpArrow   -Function HistorySearchBackward
Set-PSReadLineKeyHandler -Key DownArrow -Function HistorySearchForward
Set-PSReadLineKeyHandler -Key Tab       -Function MenuComplete
```